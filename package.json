{
  "name": "bff-deno-oak",
  "version": "1.0.0",
  "private": true,
  "description": "Backend For Frontend using Deno runner and framework Oak",
  "main": "src/index.ts",
  "scripts": {
    "start": "deno run --allow-net --allow-env --allow-read --unstable src/index.ts",
    "serve": "denon run --allow-net --allow-env --allow-read --unstable src/index.ts",
    "test": "deno test --allow-net --allow-env --allow-read --unstable",
    "lint": "deno lint",
    "format": "deno fmt",
    "bundle": "deno bundle"
  },
  "author": "marek.ganko@nokia.com",
  "license": "ISC"
}
